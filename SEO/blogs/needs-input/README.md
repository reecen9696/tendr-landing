# Parked: needs your input before it can publish

The build ignores this folder, so nothing here blocks a build.

Move a file back up to `SEO/blogs/` once it is complete, then run
`cd site && node build.mjs`.

## locations-melbourne.html

Three `{{LOCAL_CONTENT: ...}}` blocks are unwritten. They ask for genuine
Melbourne detail — the trades and project types you actually do most here, the
site conditions you actually hit, the corridors and head contractors you actually
work with.

That content cannot be written for you, and generic filler would be worse than
no page at all: a location page without real local substance is a doorway page,
which Google demotes. It is the entire reason the page would rank.

Write those three paragraphs, move the file back to `SEO/blogs/`, and it ships.
